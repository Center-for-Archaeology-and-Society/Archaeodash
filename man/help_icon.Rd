% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpHover.R
\name{help_icon}
\alias{help_icon}
\title{Add a Help Icon with Hover Text}
\usage{
help_icon(tag, text, icon_class = "fa fa-question-circle")
}
\arguments{
\item{tag}{Character or tag object. The main label or content to display.}

\item{text}{Character. The hover text that appears when the user hovers over the icon.}

\item{icon_class}{Character. CSS class for the icon. Defaults to
\code{"fa fa-question-circle"} (a Font Awesome question mark).}
}
\value{
An HTML span tag containing the label and help icon, suitable for use in
Shiny UI definitions.
}
\description{
This function creates a label with an icon (default is a question mark) that shows
a tooltip when hovered over. It is useful for adding inline help text in Shiny apps.
}
\examples{
library(shiny)

ui <- fluidPage(
    help_icon("Input Value", "Enter a numeric value here. It must be >= 0."),
    numericInput("num", NULL, 1, min = 0)
)

server <- function(input, output, session) {}

if (interactive()) shinyApp(ui, server)

}
